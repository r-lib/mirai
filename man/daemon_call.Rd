% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/next.R
\name{daemon_call}
\alias{daemon_call}
\title{Evaluate a Function Call on a Daemon}
\usage{
daemon_call(.f, ..., .compute = NULL)
}
\arguments{
\item{.f}{a function to call on the daemon.}

\item{...}{arguments passed to \code{.f}.}

\item{.compute}{(character) name of the compute profile. Each profile has its
own independent set of daemons. \code{NULL} (default) uses the 'default'
profile.}
}
\value{
The value of \code{.f} called with the supplied arguments, evaluated on a
daemon of the compute profile.
}
\description{
Evaluates a function call synchronously on a daemon of the specified compute
profile, returning its value. This is a developer interface for packages
integrating \pkg{mirai} as an evaluation backend, for example the rendering
hooks installed by \code{\link[=register_render]{register_render()}}.
}
\details{
The call is dispatched as a single \code{\link[=mirai]{mirai()}} and blocks until it resolves. If
\code{.f} accepts an \code{envir} argument and none is supplied in \code{...}, it is called
with \code{envir} set to the daemon's global environment, so that objects created
by successive calls to a \code{cleanup = FALSE} profile persist and are visible to
one another (mirroring an interactive session). Arguments in \code{...} are
evaluated in the caller and passed by value to the daemon.
}
\examples{
daemons(sync = TRUE)
daemon_call(sum, 1:10)
daemons(0)

}
