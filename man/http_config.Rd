% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launchers.R
\name{http_config}
\alias{http_config}
\title{HTTP Remote Launch Configuration}
\usage{
http_config(args = posit_workbench_info())
}
\arguments{
\item{args}{(named list) configuration for HTTP daemon launching. By default,
uses \code{posit_workbench_info()} to auto-configure from the environment. For
custom configurations, must include the following named elements:
\itemize{
\item \code{url} (character) URL endpoint for the launch API (required).
\item \code{method} (character) HTTP method, typically \code{"POST"} (required).
\item \code{cookie} (character or function) session cookie value. May be a
function returning the cookie value (optional, if required for
authentication).
\item \code{token} (character or function) authentication bearer token. May be a
function returning the token value. (optional, if required for
authentication).
\item \code{data} (character) JSON or formatted request body containing the daemon
launch command. Should include a placeholder \code{"\%s"} where the host URL
will be inserted at launch time (required).
}}
}
\value{
A list in the required format to be supplied to the \code{remote} argument
of \code{\link[=daemons]{daemons()}} or \code{\link[=launch_remote]{launch_remote()}}.
}
\description{
Generates a remote configuration for launching daemons via HTTP API. By
default, automatically configures for Posit Workbench using environment
variables.
}
\examples{
tryCatch(http_config(), error = identity)

# Custom HTTP configuration example:
http_config(
  args = list(
    url = "https://api.example.com/launch",
    method = "POST",
    cookie = function() Sys.getenv("MY_SESSION_COOKIE"),
    token = function() Sys.getenv("MY_API_KEY"),
    data = '{"command": "mirai::daemon(\"\%s\")"}'
  )
)

\dontrun{
# Launch 2 daemons using http config default (for Posit Workbench):
daemons(n = 2, url = host_url(), remote = http_config())
}

}
\seealso{
\code{\link[=ssh_config]{ssh_config()}}, \code{\link[=cluster_config]{cluster_config()}} and \code{\link[=remote_config]{remote_config()}} for other
types of remote configuration.
}
