% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daemons.R
\name{dispatcher_capacity}
\alias{dispatcher_capacity}
\title{Dispatcher Capacity}
\usage{
dispatcher_capacity(.compute = NULL)
}
\arguments{
\item{.compute}{(character) name of the compute profile. Each profile has its
own independent set of daemons. \code{NULL} (default) uses the 'default'
profile.}
}
\value{
Named numeric vector of length 3: \strong{used} (current) and \strong{peak}
(high-watermark) usage, and \strong{capacity} (the budget set in \code{\link[=daemons]{daemons()}},
\code{NA_real_} if unset/unbounded), all in MB. \code{NULL} if the compute profile
is not using dispatcher.
}
\description{
Retrieve the approximate current and peak memory used by queued task
payloads at dispatcher, in MB (metric, 1 MB = 1,000,000 bytes), to monitor
queue pressure against the \code{capacity} budget set in \code{\link[=daemons]{daemons()}}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
daemons(1, capacity = 100)
m <- mirai(Sys.sleep(0.5))
dispatcher_capacity()
m[]
daemons(0)
\dontshow{\}) # examplesIf}
}
