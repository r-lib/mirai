% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr.R
\name{register_knitr}
\alias{register_knitr}
\title{Evaluate knitr Chunks on Daemons}
\usage{
register_knitr()
}
\value{
Invisible NULL. Called for the side effect of installing the hook.
}
\description{
Registers a \pkg{knitr} \code{evaluate} hook so that R code chunks tagged with a
\code{compute} chunk option are evaluated on a mirai daemon rather than in the
host session. This provides a lightweight way to run selected chunks of a
\pkg{knitr}, R Markdown or Quarto document on parallel or remote daemons.
}
\details{
Set up daemons for one or more compute profiles using \code{\link[=daemons]{daemons()}} before
rendering, for example \code{daemons(4, .compute = "gpu")}. Then call
\code{register_knitr()} once, typically in a setup chunk. Thereafter, for each R
code chunk:
\itemize{
\item \verb{#| compute: gpu} evaluates the chunk on the \code{"gpu"} compute profile.
\item \verb{#| compute: true} evaluates the chunk on the \code{"default"} profile.
\item An untagged chunk evaluates in the host session, unchanged.
}

Chunks routed to the same profile share state sequentially, as each is
evaluated in the daemon's global environment. Chunks on different profiles,
and the host session, are isolated from one another. Inline R code always
evaluates in the host session.

\pkg{knitr} is loaded on the daemons of a profile the first time a chunk is
routed there, hence each such daemon requires \pkg{knitr} and \pkg{evaluate}
to be installed. Graphics produced on a daemon are recorded and replayed on
the host, so figures are written as usual.

Calling \code{register_knitr()} more than once is safe: it does not re-wrap its
own hook, and each call resets the record of which profiles have been
prepared, so a fresh render re-loads \pkg{knitr} on the daemons as needed.
}
\examples{
\dontrun{
# in a setup chunk of a knitr / R Markdown / Quarto document:
library(mirai)
daemons(4, .compute = "gpu")
register_knitr()
}

}
