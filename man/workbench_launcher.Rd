% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launchers.R
\name{workbench_launcher}
\alias{workbench_launcher}
\title{Workbench Job Launcher Configuration}
\usage{
workbench_launcher(
  rscript = "Rscript",
  job_name = "mirai_job",
  cluster = NULL,
  resource_profile = NULL,
  cpus = 1,
  memory = 512
)
}
\arguments{
\item{rscript}{(character) Rscript executable path.}

\item{job_name}{(character) base name for launched jobs (a timestamp and
random suffix are appended automatically).}

\item{cluster}{(character or NULL) name of the cluster to use. \code{NULL}
(default) uses the first available cluster.}

\item{resource_profile}{(character or NULL) named resource profile (e.g.
\code{"rstudio"}). \code{NULL} (default) uses custom resource values instead.}

\item{cpus}{(integer) number of CPUs for custom resources (ignored if
\code{resource_profile} is set).}

\item{memory}{(integer) memory in MB for custom resources (ignored if
\code{resource_profile} is set).}
}
\value{
A function suitable for the \code{data} argument of \code{\link[=http_config]{http_config()}}.
}
\description{
Creates a custom launch configuration for Posit Workbench with control over
cluster selection, resource profiles, and resource allocation. Returns a
function suitable for use as the \code{data} argument of \code{\link[=http_config]{http_config()}}.
}
\details{
Either supply \code{resource_profile} to use a named profile, or specify custom
\code{cpus} and \code{memory}. If \code{resource_profile} is set, \code{cpus} and \code{memory} are
ignored.
}
\examples{
# Use a named resource profile on a specific cluster:
http_config(
  data = workbench_launcher(
    resource_profile = "rstudio",
    cluster = "Kubernetes"
  )
)

# Specify custom resources (4 CPUs, 8 GB memory):
http_config(
  data = workbench_launcher(
    job_name = "my_analysis",
    cpus = 4,
    memory = 8192
  )
)

\dontrun{
# Launch 2 daemons using default settings:
daemons(
  n = 2,
  url = host_url(),
  remote = http_config(
    data = workbench_launcher(
      resource_profile = "rstudio",
      cluster = "Kubernetes"
    )
  )
)
}

}
\seealso{
\code{\link[=http_config]{http_config()}} for the configuration that accepts this as its
\code{data} argument.
}
